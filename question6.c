#include<stdio.h>
// write a program which takes a character as an input display its ASCII code?
int main()
{
    char ch;
    printf("enter is the character ");
    scanf("%c",&ch);
    printf("character is ASCII code is %d",ch);
    return 0;

}